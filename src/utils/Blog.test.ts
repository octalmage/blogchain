//import Blog from './Blog.ts'

//it('gets a list of blog posts by address', () => {
//  let web3 = null;
//  let blog = new Blog(web3);
//  let author = 1;
//  posts = blog.getBlogPostsByAuthor(author);
//  expect(posts.count).toEqual(100);
//});
